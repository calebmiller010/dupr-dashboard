{
  "buildCommand": "cd src/ui && npm install && npm run build",
  "outputDirectory": "src/ui/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" }
  ]
}
